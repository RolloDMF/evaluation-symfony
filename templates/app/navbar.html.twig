<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('home') }}">Un super Blog</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="{{ path('home') }}">Home</a>
      <a class="nav-item nav-link" href="{{ path('articles_index') }}">Articles</a>
      {% if is_granted('ROLE_USER') %}
        <a class="nav-item nav-link" href="{{ path('user_show', {'id': app.user.id}) }}">Profil</a>
        <a class="nav-item nav-link" href="{{ path('app_logout') }}">Se deconnecter</a>
      {% else %}
        <a class="nav-item nav-link" href="{{ path('user_new') }}">S'inscrire</a>
        <a class="nav-item nav-link" href="{{ path('app_login') }}">Se connecter</a>
      {% endif %}
    </div>
  </div>
</nav>